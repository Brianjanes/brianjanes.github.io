---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-xl py-1">Hey there 👋</div>
    <div class="text-5xl font-bold">I'm Brian Janes</div>
    <div class="text-3xl py-3 font-bold">Software Developer</div>
    <div class="py-2">
      <text class="text-lg">
        I'm a dedicated professional with a background in culinary arts and a
        passion for web development. With a decade in the culinary world, I've
        mastered organization, time management, and effective communication.
        Transitioning to web development, I'm proficient in technologies like
        React and Node, driven by a commitment to innovation and collaboration.
        My unique blend of skills, from leadership to technical prowess, equips
        me to create impactful solutions that positively impact lives. I'm
        excited to contribute my expertise to projects that matter and promote
        positive change within the tech community.
      </text>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">My latest projects {"</>"}</div>
  </div>

  <HorizontalCard
    title="Password Generator"
    img="/pw-gen.png"
    desc="Full-Stack password generator web app built with MERN stack. Able to generate and save passwords of varying lengths and complexities with labels indicating what they are used for."
    url="https://github.com/Brianjanes/password-generator"
    badge="NEW"
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="AI-Sommelier"
    img="/perfect-pairing.png"
    desc="React web app that uses openAI to take a meal that the user wants to have and reccomends a few different wine pairing options at varying price points."
    url="https://github.com/Brianjanes/perfect-pairing"
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="DOGWALKR"
    img="/dogwalkr.png"
    desc="My first MERN stack web app. A social media site that is intended to help people connect for dog walks more easily. Using google-maps-react library to hone in a the user's location, and Auth0 for authentication."
    url="https://github.com/Brianjanes/dogwalkr"
    badge=""
  />

  <!-- <div>
    <div class="text-3xl w-full font-bold mb-5 mt-10">Latest from blog</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.data.title}
          img={post.data.heroImage}
          desc={post.data.description}
          url={"/blog/" + post.slug}
          target="_self"
          badge={post.data.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  } -->
</BaseLayout>
